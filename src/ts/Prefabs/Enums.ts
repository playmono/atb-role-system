export enum AreaOfEffect {
    Single,
    Row,
    Group
}

export enum EffectRange {
    Self = 0,
    One = 1,
    Two = 2,
    Three = 3
}

export enum DamageType {
    Physical,
    Magical,
    None
}
